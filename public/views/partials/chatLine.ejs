<p class="chat-line chat-<%= msg.type %>">
  <% if (msg.type === 'system') { %>
    <%= msg.body %>
  <% } else if (msg.type === 'emote') { %>
    <span class="sender"><%= msg.sender_name %></span> <%= msg.body %>
  <% } else { %>
    <span class="sender"><%= msg.sender_name %>:</span> <%= msg.body %>
  <% } %>
</p>
